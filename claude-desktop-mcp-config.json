{
  "mcpServers": {
    "tui-control": {
      "command": "python",
      "args": [
        "-c",
        "import requests; import json; import sys; url = 'http://127.0.0.1:8089/mcp'; requests.post(url, json={'method': 'initialize', 'params': {'protocolVersion': '2024-11-05', 'capabilities': {'tools': {}}}})"
      ],
      "env": {},
      "disabled": false
    }
  }
}